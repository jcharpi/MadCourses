# ================================================================
# MadCourses - Ultra Minimal for Vercel (No scipy/scikit-learn)
# ================================================================
# Uses HuggingFace Inference API + manual cosine similarity
# Total size: ~15MB (vs 250MB+ with PyTorch)
# ================================================================

# ─── Essential Only ────────────────────────────────────────────
numpy==2.2.6                    # Array operations (~8MB)
requests==2.31.0                # HTTP client for HF API (~1MB)